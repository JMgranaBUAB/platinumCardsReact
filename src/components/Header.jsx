import React from 'react';

const Header = ({ totalCount }) => {
    return (
        <header>
            <h1>Los {totalCount} Platinos de JMgranaGaming</h1>
        </header>
    );
};

export default Header;
